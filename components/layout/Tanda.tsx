/** Letter-spaced condensed wordmark, after the reference's "S M A R T". */
export function Tanda({ className = "" }: { className?: string }) {
  return (
    <span
      className={`font-display text-xl font-extrabold tracking-[0.42em] text-tinta uppercase ${className}`}
    >
      Nayba
    </span>
  );
}
